WAI Studio - 아키텍처 & 작업 방식 요약
📐 1. 프로젝트 아키텍처
1.1 디렉토리 구조
C:\wai-ui\
│
├── frontend\                           # 프론트엔드 루트
│   │
│   ├── index.html                      # Vue 3 마운트 포인트 (100 lines)
│   │
│   ├── css\                            # 스타일시트 디렉토리
│   │   ├── main.css                    # CSS 진입점 (@import 통합, 50 lines)
│   │   ├── global.css                  # 전역 스타일 + CSS 변수 (80 lines)
│   │   ├── timeline.css                # Timeline 컴포넌트 스타일 (150 lines)
│   │   ├── canvas.css                  # Canvas 컴포넌트 스타일 (120 lines)
│   │   ├── panels.css                  # Header/LeftPanel/RightPanel 스타일 (180 lines)
│   │   └── components.css              # 공통 UI 컴포넌트 스타일 (100 lines)
│   │
│   └── js\                             # JavaScript 디렉토리
│       ├── app.js                      # Vue 3 애플리케이션 초기화 (150 lines)
│       ├── store.js                    # 중앙 집중식 상태 관리 (250 lines)
│       │
│       ├── components\                 # Vue 컴포넌트 디렉토리
│       │   ├── Common.js               # Dropdown, Modal, ContextMenu (300 lines)
│       │   ├── Timeline.js             # Timeline 메인 컴포넌트 (500 lines)
│       │   ├── Canvas.js               # PreviewCanvas 메인 컴포넌트 (500 lines)
│       │   └── Panels.js               # Header, LeftPanel, RightPanel (400 lines)
│       │
│       └── utils.js                    # 유틸리티 함수 모음 (200 lines)
│
├── backend\                            # 파이썬 백엔드 (Phase 3 예정)
│   ├── main.py                         # FastAPI 서버
│   ├── render.py                       # FFmpeg 비디오 렌더링
│   └── templates\                      # 템플릿 JSON 저장소
│
├── wai_magic.py                        # Magic Sync 자동화 스크립트
├── PROJECT_MAP.md                      # 프로젝트 구조 문서
├── DESIGN_GUIDE.md                     # 디자인 시스템 가이드
└── TECH_SPECS.md                       # 기술 명세서

총 파일 개수: 14개 (HTML 1 + CSS 6 + JS 7)
평균 파일 크기: 150~250 lines
최대 파일 크기: 500 lines (Timeline.js, Canvas.js)


1.2 아키텍처 패턴
Frontend Architecture
┌─────────────────────────────────────────────────────────────┐
│                         index.html                          │
│                  (Vue 3 마운트 포인트)                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                         app.js                              │
│              (Vue 3 createApp + 컴포넌트 등록)              │
└─────────────────────────────────────────────────────────────┘
                              ↓
        ┌─────────────────────┴─────────────────────┐
        ↓                                           ↓
┌───────────────────┐                   ┌───────────────────┐
│    store.js       │                   │   components/     │
│ (Reactive State)  │ ←────────────────→│  - Timeline.js    │
│                   │                   │  - Canvas.js      │
│  - projectName    │                   │  - Panels.js      │
│  - tracks[]       │                   │  - Common.js      │
│  - clips[]        │                   └───────────────────┘
│  - canvasBoxes[]  │                              ↓
│  - selectedLayerId│                   ┌───────────────────┐
└───────────────────┘                   │     utils.js      │
                                        │  - formatTime()   │
                                        │  - hexToRgb()     │
                                        │  - snapToGuide()  │
                                        └───────────────────┘
패턴명: Flux-like Pattern (중앙 집중식 상태 관리)

특징:

단방향 데이터 흐름: Component → Store → Component
전역 상태 관리: store.js (Vue Reactive)
컴포넌트 통신: this.$store 전역 접근
CSS Architecture
┌─────────────────────────────────────────────────────────────┐
│                        main.css                             │
│                   (@import 진입점)                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
        ┌─────────────────────┼─────────────────────┐
        ↓                     ↓                     ↓
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│  global.css   │   │ timeline.css  │   │  canvas.css   │
│               │   │               │   │               │
│ - CSS 변수    │   │ - .clip       │   │ - .canvas-box │
│ - Scrollbar   │   │ - .playhead   │   │ - .guide-line │
│ - Animation   │   │ - .waveform   │   │ - .box-handle │
└───────────────┘   └───────────────┘   └───────────────┘
        ↓                     ↓                     ↓
┌───────────────┐   ┌───────────────────────────────────┐
│  panels.css   │   │      components.css               │
│               │   │                                   │
│ - .c-header   │   │ - .c-dropdown                     │
│ - .c-left-    │   │ - .modal-overlay                  │
│   panel       │   │ - .context-menu                   │
│ - .c-right-   │   │ - .tool-btn                       │
│   panel       │   │                                   │
└───────────────┘   └───────────────────────────────────┘
패턴명: Modular CSS (기능별 분리 + @import 통합)

네이밍 규칙: BEM (Block Element Modifier)

Block: .c-header
Element: .c-header__logo
Modifier: .c-header__btn--primary

🛠️ 2. 기술 스택
2.1 Core Technologies
카테고리	기술	버전	사용 방식
프레임워크	Vue.js	3.x	CDN (https://unpkg.com/vue@3/dist/vue.global.js)
API 스타일	Options API	-	data(), computed, methods 방식
모듈 시스템	ES6 Modules	ES2015+	import / export
상태 관리	Vue Reactive	-	const { reactive } = Vue
스타일 프레임워크	Tailwind CSS	3.x	CDN + JIT 모드
커스텀 CSS	Modular CSS	-	BEM 네이밍 + @import
2.2 External Libraries
라이브러리	용도	CDN
SweetAlert2	모달, 토스트, 알림	https://cdn.jsdelivr.net/npm/sweetalert2@11
Interact.js	Drag & Drop, Resize	https://cdn.jsdelivr.net/npm/interactjs/dist/interact.min.js
Font Awesome	아이콘	https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css
2.3 Development Tools
도구	용도
VS Code	코드 에디터
Live Server	로컬 개발 서버 (http://localhost:5500)
Chrome DevTools	브라우저 디버깅 (F12)
Git	버전 관리
Magic Sync (wai_magic.py)	자동 저장 + 커밋 + Push
📝 3. 작업 방식 (Workflow)
3.1 Magic Sync 개요
Magic Sync는 클립보드를 감시하여 자동으로 파일을 저장하고 Git에 커밋/Push하는 Python 스크립트입니다.

Copy# wai_magic.py 핵심 로직
while True:
    content = pyperclip.paste()  # 클립보드 내용 읽기
    
    if content != LAST_CONTENT:
        match = re.search(r'### \[WAI:UPDATE:(.*?)\]', content)
        
        if match:
            target_file = match.group(1).strip()
            clean_code = re.sub(r'### \[WAI:UPDATE:.*?\].*', '', content, count=1)
            
            # 파일 저장
            save_file(target_file, clean_code)
            
            # Git 커밋 + Push
            git_commit_and_push(target_file)
3.2 개발 프로세스
┌─────────────────────────────────────────────────────────────┐
│  Step 1: PowerShell로 폴더 구조 생성                        │
│  → Rename-Item, New-Item 명령 실행                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 2: Magic Sync 실행                                    │
│  → python wai_magic.py                                      │
│  → 클립보드 감시 시작                                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 3: AI가 파일 코드 제공                                 │
│  → ### [WAI:UPDATE:css/timeline.css]                       │
│  → (코드 내용)                                               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 사용자가 코드 복사 (Ctrl+C)                        │
│  → 클립보드에 코드 저장                                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 5: Magic Sync 자동 처리                               │
│  → 파일 저장 (C:\wai-ui\frontend\css\timeline.css)        │
│  → Git 커밋 ("[WAI:Magic] css/timeline.css")               │
│  → GitHub Push (https://github.com/codeboy138/wai_ui)      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 6: 브라우저에서 확인                                   │
│  → http://localhost:5500/index.html                         │
│  → F5 (새로고침)                                             │
└─────────────────────────────────────────────────────────────┘

3.3 파일 작업 순서
Phase 1: 기본 구조 (5개 파일)
순서	파일	크기	내용
1	index.html	100 lines	Vue 3 마운트 포인트 + CDN 로드
2	css/main.css	50 lines	@import로 모든 CSS 통합
3	css/global.css	80 lines	전역 스타일 + CSS 변수 + Scrollbar
4	js/app.js	150 lines	Vue 3 createApp + 컴포넌트 등록
5	js/store.js	250 lines	Reactive 전역 상태 관리
Phase 2: CSS 스타일 (4개 파일)
순서	파일	크기	내용
6	css/timeline.css	150 lines	Timeline 컴포넌트 스타일
7	css/canvas.css	120 lines	Canvas 컴포넌트 스타일
8	css/panels.css	180 lines	Header, LeftPanel, RightPanel 스타일
9	css/components.css	100 lines	Dropdown, Modal, ContextMenu 스타일
Phase 3: Vue 컴포넌트 (5개 파일)
순서	파일	크기	내용
10	js/components/Common.js	300 lines	Dropdown, Modal, ContextMenu 컴포넌트
11	js/components/Timeline.js	500 lines	Timeline 메인 컴포넌트 + Clip 로직
12	js/components/Canvas.js	500 lines	Canvas 메인 컴포넌트 + Box 로직
13	js/components/Panels.js	400 lines	Header, LeftPanel, RightPanel 컴포넌트
14	js/utils.js	200 lines	formatTime, hexToRgb, snapToGuide 등
🔑 4. 핵심 개념
4.1 ES6 Modules
정의: ECMAScript 2015에서 도입된 JavaScript 표준 모듈 시스템.

사용법:

Copy// store.js (내보내기)
const store = reactive({ ... });
export default store;

// Timeline.js (가져오기)
import store from '../store.js';
장점:

✅ 파일 간 명확한 의존성 관리
✅ 코드 재사용 (중복 제거)
✅ 네이밍 충돌 방지 (모듈 스코프)
4.2 Options API
정의: Vue 2 방식의 컴포넌트 정의 API (Vue 3에서도 지원).

구조:

Copyexport default {
  name: 'Timeline',
  
  data() {
    return { pixelsPerSecond: 50 };
  },
  
  computed: {
    tracks() { return this.$store.tracks; }
  },
  
  methods: {
    handlePlay() { this.$store.isPlaying = true; }
  },
  
  mounted() {
    this.initInteract();
  },
  
  template: `<div>...</div>`
};
특징:

섹션별 명확한 구분 (data, computed, methods)
this 키워드로 컴포넌트 내부 접근
Vue 2 경험자에게 친숙
4.3 Reactive State (Vue Reactive)
정의: Vue 3의 반응형 상태 관리 API.

사용법:

Copy// store.js
const { reactive } = window.Vue;

const store = reactive({
  projectName: 'Untitled',
  clips: []
});

export default store;

// 컴포넌트에서 접근
this.$store.clips.push(newClip);  // 자동 UI 업데이트
특징:

상태 변경 시 자동으로 UI 업데이트
중앙 집중식 상태 관리 (Vuex 대체)
4.4 BEM (Block Element Modifier)
정의: CSS 클래스 네이밍 규칙.

규칙:

Copy/* Block */
.c-header { ... }

/* Element (Block의 하위 요소) */
.c-header__logo { ... }
.c-header__title { ... }

/* Modifier (상태 변화) */
.c-header__btn--primary { ... }
.c-header__btn--disabled { ... }
장점:

✅ 클래스 이름만으로 구조 파악 가능
✅ 네이밍 충돌 방지
✅ 재사용성 향상

4.5 DATA-DEV 속성
정의: HTML 요소에 개발자 가이드 정보를 담는 커스텀 속성.

형식 (7가지 항목):

Copy<button
  id="btn-save"
  class="c-header__btn"
  data-dev="
요소의 역할: 프로젝트 저장 버튼
요소의 고유ID: btn-save
요소의 기능 목적 정의: 클릭 시 현재 프로젝트를 파일로 저장
요소의 동작 로직 설명: handleSave() 호출 → store 직렬화 → 파일 저장 API 호출
요소의 입출력 데이터 구조: 입력: store 전체 상태. 출력: 저장 성공/실패 상태
요소의 경로정보: frontend/js/components/Panels.js#Header → SaveButton
요소의 수행해야 할 백엔드/JS 명령: JS: this.handleSave(), fetch('/api/save', ...)
  "
>
  Save
</button>
활용:

마우스 오버 시 Inspector 패널에 상세 정보 표시
코드 검토 시 빠른 이해 가능
📊 5. 아키텍처 의사결정 (ADR)
5.1 왜 Options API인가?
기준	Options API	Composition API
학습 곡선	⭐⭐⭐⭐⭐ (쉬움)	⭐⭐⭐ (중간)
코드 구조	섹션별 명확 구분	setup() 내부 집중
CDN 호환성	⭐⭐⭐⭐⭐ (완벽)	⭐⭐⭐ (구조 분해 필요)
프로젝트 규모	중소규모 최적	대규모 최적
결론: 본 프로젝트는 중소규모 + 빠른 프로토타입이므로 Options API가 최적.

5.2 왜 ES6 Modules인가?
기준	전역 함수 방식	ES6 Modules
코드 재사용	❌ 중복 발생	✅ import 재사용
네이밍 충돌	⚠️ window.* 오염	✅ 모듈 스코프
파일 분리	❌ 모두 한 파일에	✅ 기능별 독립 파일
유지보수성	⭐⭐⭐	⭐⭐⭐⭐⭐
결론: 파일 길이 제약 + 코드 재사용을 위해 ES6 Modules 필수.

5.3 왜 CSS 파일을 분리했는가?
기준	단일 app.css	분리 (6개 파일)
파일 크기	600 lines	80~180 lines
복사 편의성	⚠️ 스크롤 길이	✅ 쉬움
기능별 수정	❌ 검색 필요	✅ 파일 바로 찾기
유지보수성	⭐⭐	⭐⭐⭐⭐⭐
결론: 기능별 CSS 분리로 개발 속도 향상 + 유지보수 편의성 확보.

🚀 6. 다음 단계
6.1 PowerShell 명령 실행
Copy# 1. 기존 frontend 폴더 백업
Rename-Item -Path "C:\wai-ui\frontend" -NewName "frontend_v2"

# 2. 새로운 frontend 폴더 구조 생성
New-Item -ItemType Directory -Force -Path "C:\wai-ui\frontend"
New-Item -ItemType Directory -Force -Path "C:\wai-ui\frontend\css"
New-Item -ItemType Directory -Force -Path "C:\wai-ui\frontend\js"
New-Item -ItemType Directory -Force -Path "C:\wai-ui\frontend\js\components"

Write-Host "✅ ES6 Modules 구조 생성 완료!" -ForegroundColor Green
6.2 Magic Sync 실행
Copycd C:\wai-ui
python wai_magic.py
콘솔 출력:

=============================================
   🎩 WAI Magic Sync v3.0 (Hybrid Edition)
   📂 로컬: C:\wai-ui\frontend
   🌐 GitHub: https://github.com/codeboy138/wai_ui
=============================================

🔍 [Git 환경 검증 시작]
   ✅ Git 설치: git version 2.x.x
   ✅ Git 저장소 초기화됨
   ✅ Git 사용자: codeboy138 <email@example.com>
   ✅ 원격 저장소 연결됨
✅ [Git 환경 검증 완료]

   🔥 커밋 + 🚀 Push 활성

   📡 [대기 중] 클립보드 감시...
   ✅ 지원 파일:
      - index.html
      - css/*.css (6개)
      - js/app.js
      - js/store.js
      - js/components/*.js (4개)
      - js/utils.js
=============================================
6.3 파일 생성 시작
사용자 입력: 완료

AI 응답:
Phase 1-1 시작 → index.html 코드 제공 (### [WAI:UPDATE:index.html])

✅ 7. 요약
항목	내용
총 파일 개수	14개 (HTML 1 + CSS 6 + JS 7)
평균 파일 크기	150~250 lines
모듈 시스템	ES6 Modules (import/export)
Vue API	Options API (data, computed, methods)
상태 관리	Vue Reactive (중앙 집중식)
CSS 아키텍처	Modular CSS + BEM
자동화 도구	Magic Sync (Python 스크립트)
개발 서버	Live Server (http://localhost:5500)
Git 저장소	https://github.com/codeboy138/wai_ui
